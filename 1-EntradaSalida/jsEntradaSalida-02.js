/*
Debemos lograr tomar un nombre con 'prompt' 
y luego mostrarlo por 'alert' al presionar el botón  'mostrar'*/
function mostrar()
{
	//declaración
	var nombre;
	//asignación
	nombre = prompt ("Registre su nombre: "); //Al dar igual doy inicio a la variable. = es operador de asignación.
	//mostrando lo que hay en la variable
	alert (nombre); //lo que esta dentro de las comillas se llama cadena de texto 
	// una constante es una variable de solo lectura. No se lo puede modificar una vez asignada.

}

